# SKILLS / プロジェクト固有ルール

このプロジェクトにおけるカテゴリ間の独立性と共有ユーティリティの変更に関する厳格なルールです。

## 1. 共有ユーティリティ（stringUtils.ts 等）の変更禁止
`shortenProductName` などの関数は、複数のカテゴリ（別注、SP等）で共有されています。
- **ルール**: 特定のカテゴリ（例：SP）の表示を改善するために、これらの共有関数のロジック（正規表現等）を直接変更しないでください。
- **理由**: 他のカテゴリ（例：別注）で既に確立されている表示ルールが崩れる原因になります。
- **対策**: カテゴリ固有のクリーニングが必要な場合は、`calculator.ts` 内やコンポーネント内で個別に処理するか、カテゴリ専用の関数を新設してください。

## 2. カテゴリごとのロジック分離
`calculator.ts` や `excelUtils.ts` での変更は、必ずカテゴリ判定（`isSP` 等）で囲み、他への波及を防いでください。

## 3. 「別注」カテゴリの現状維持
「別注」の商品名表示については、以下の既存ルールを維持してください。
- **参照元**: Excelの「商品名」列（カラムE）を使用。
- **加工**: `shortenProductName` を適用。

## 4. 「SP」カテゴリの表示ルール
- **参照元**: Excelの「タイトル」列（カラムU）を使用。
- **加工**: `calculator.ts` 内の `cleanSPTitle` で冗長な情報を除去。
- **UI表示**: UI側では `shortenProductName` を通さず、クリーニング済みの名称をそのまま表示（【 】等の重要な材質情報を残すため）。
