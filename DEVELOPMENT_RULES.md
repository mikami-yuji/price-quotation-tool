# 開発重要ルール (Regression Prevention)

本プロジェクトにおいて、以下のロジックは業務上極めて重要であり、**簡略化や削除を固く禁じます。**

## 1. 品名短縮（ピーリング）ロジック
- **ファイル**: `src/utils/stringUtils.ts` 内の `shortenProductName`
- **理由**: 受注データの複雑なスペック表記を剥ぎ取り、見積書にふさわしい銘柄名を抽出するため。
- **変更時のルール**:
    1. 修正を加える場合は、必ず `src/utils/stringUtils.test.ts` を実行し、既存のテストケース（24件以上）がすべてパスすることを確認すること。
    2. 新しいパターンを追加する場合は、必ずテストコードにもそのケースを追加すること。
    3. `normalize('NFKC')` による正規化処理は、表記の揺れを抑えるために必須である。

## 2. マスター照合ロジック
- **ファイル**: `src/utils/calculator.ts`
- **理由**: カテゴリごとの複雑な単価決定ロジック（SPのコード解析、別注の材質マトリクス等）を司るため。
- **変更時のルール**: `src/utils/calculator.test.ts` の全テスト（15件以上）をパスさせること。

## 3. 型定義の遵守
- 全ての記録は `src/types/index.ts` の `OrderRecord` 型に準拠させること。
- プロダクションビルド（Vercel）の成功を最優先し、型エラーを無視してはならない。

---
これらのルールは、AIエージェントがセッションを跨いで作業する際の「記憶の継承」として機能します。
変更を行う前に、必ずこれらのドキュメントとテストコードを読み込んでください。
