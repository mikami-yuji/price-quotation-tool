const XLSX = require('xlsx');
require('ts-node/register'); // if we need to require ts files directly, but we can just use the compiled one or we can just copy the logic.
// actually let's just write a pure JS runner for the logic, or compile it.
