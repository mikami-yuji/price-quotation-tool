## [Unreleased] - 2026-05-19
### Fixed
- **既製品客層区分「D」価格マッチングのバグ修正**:
    - `excelUtils.ts`: 列ヘッダー判定 `getIdx` に完全一致の優先と「除外キーワード」機能を追加。これにより、「改定後 準D」の列ヘッダーが「D」の列判定に誤マッチするバグを解決。「D」を選択した際に正しく「D」の価格（準Dの価格ではなく）が引用・表示されるように修正。
- **価格タイプ（通常/CP）の伝達バグ修正**:
    - `usePriceSimulation.ts`: 計算処理 `calculateNewPrices` に、画面側の状態変数 `readymadePriceType` を渡すように修正。これにより、通常価格とCP価格の切り替えが正しく反映されるよう改善。

## [Unreleased] - 2026-05-18
### Added
- **ソート機能の向上**:
    - `useFilters.ts`: シミュレーションおよび表示用の一覧データのソート順に「受注数」を追加。これにより「材質 → 重量 → 色数 → 受注数（昇順）」の規則で整列されるようになり、Excel出力時にもこの順序が反映されるように改善。

## [Unreleased] - 2026-05-10
### Added
- **品名短縮ロジックの統合と拡張**: 
    - `stringUtils.ts`: 品名から仕様（重量・材質コード等）を確実に抽出する「ピーリング（剥ぎ取り）ループ」を実装。
    - アプリケーション全体でSP商品の判定基準を統一するための `isSPCategory` ヘルパーを導入。
    - SP商品に加えて「別注（Betchu）」商品に対しても品名短縮を適用し、表示の整合性を向上。

### Refactored
- **計算エンジン (`calculator.ts`) の抜本的リファクタリング**: 
    - 巨大なメインループをカテゴリ（SP/既製品/別注）ごとの明確なロジックに整理。
    - 全ての内部関数に明示的な戻り値の型を指定し、`any` 型を排除。
    - 厳格な定数化（`let` から `const` への移行）を徹底し、再代入エラーを根本的に解消。

### Fixed
- **Vercel ビルドエラーの解消**: 
    - `index.ts`: `OrderRecord` 型に不足していた `matchMethod` および `masterPrice` を追加。
    - `calculator.ts`: 型不整合（`priceDiff` 削除、`weight` の数値キャスト追加）を修正し、プロダクションビルドが通る状態へ復旧。
- **ユニットテストの修正**:
    - `calculator.test.ts`: SP商品の材質不一致テストにおいて、デフォルト増分が欠落していた問題を修正し、15件すべてのテストをパスするように調整。
- **デバッグコードの除去**: 
    - 全ての `console.log` を削除し、プロジェクト規定を遵守。

## [Unreleased] - 2026-05-07
### Added
- **ESLint設定の最適化**: 
    - `eslint.config.mjs`: `scratch/` ディレクトリを lint 対象外に設定し、一時スクリプトによるビルド失敗を防止。
- **計算ロジックの堅牢化**: 
    - `calculator.ts`: 引数のデフォルト値を強化し、欠損データ時も `NaN` や `TypeError` を発生させないガードを追加。

### Fixed
- **ユニットテストの完全復旧**:
    - `calculator.test.ts`: 新しい関数シグネチャ（`priceMatrix` 削除）に合わせて15件のテストケースをすべて修正。
    - `calculator.ts`: 丸め処理適用時も営業Gの計算に「理想増分」を使用するようにし、テスト期待値との不一致を解消。
    - `calculator.ts`: SPマスターマッチングにおける全角正規化とカタログ番号照合を強化し、正解率を向上。
- **ビルド警告の解消**: 
    - 未使用引数の削除および React Hook 依存関係の整理を行い、 lint 警告をゼロに。

## [Unreleased] - 2026-05-06 (Update 3)

### Added
- **SP色数判定の高度化**: 
    - `calculator.ts`: 印刷コードを優先的に解析し、「2色」「3色」などの指定を自動抽出するロジックを実装。
    - キーワードマッチングの導入: 「ＳＰロール印刷」「ロール印刷代」「ロール印刷」が含まれる場合、明示的な色数指定がなくても自動的に「1色」として扱うように改善。
    - 色数カラム（受注データの「色数」等）をフォールバックとして活用する柔軟な判定フローを構築。

### Fixed
- **SPマスターパーサーの堅牢化**:
    - `excelUtils.ts`: アルファベットを含むカタログ番号（例: SP-ZIP, RZ-123）が解析から漏れていた問題を修正。
    - `excelUtils.ts`: 受注データの色数カラム等における「1+1」形式の表記を自動合算（1+1=2）する数値抽出ロジックを実装。
    - `excelUtils.ts`: 単価セル内の通貨記号（￥, $）や、価格タイブラベル（売、準、D）の判定ロジックを緩和し、多様なExcelレイアウトに対応。
- **ハイドレーションエラーの抑制**:
    - `layout.tsx`: `suppressHydrationWarning` を追加し、環境依存のクラス注入によるSSR/クライアント間の不一致警告を解消。

## [Unreleased] - 2026-05-06 (Update 2)

### Fixed
- **SPマスターの「材質ヒント」抽出ロジックの修正**: 
    - `excelUtils.ts`: マルチテーブル対応時に欠落していた材質（【 】内の商品名など）の抽出ロジックを再実装。価格データと材質を正確に紐付けられるように修正。
- **SPマッチングの安定化**: 材質ヒントが正しくマスターに登録されるようになったことで、計算エンジン（`calculator.ts`）での自動マッチング精度を復元。

## [Unreleased] - 2026-05-06 (Update)

### Added
- **SKILLS.mdの作成**: カテゴリ間のロジック隔離、共有ユーティリティの変更禁止、および「別注」カテゴリーの既存ルール死守を明文化したプロジェクト規約を策定。

### Fixed
- **SP商品の「タイトル」ベース表示の実現**: 
    - `excelUtils.ts`: ExcelのカラムU（タイトル）を全商品で取得するように拡張。
    - `calculator.ts`: SP商品に限定して、タイトルから日付や受注番号を除去する専用クリーニングロジック（`cleanSPTitle`）を実装。
- **「別注」カテゴリーの表示ルール復元**: 
    - `OrderDataTable.tsx`: SP以外のカテゴリー（特に「別注」）において、新設されたタイトル列ではなく、従来通り「商品名」列を使用して表示・短縮（`shortenProductName`）を行うようにロジックを分離。これにより、意図しない表示ルールの変更を解消。

## [Unreleased] - 2026-05-06

### Changed
- **SPマスターパーサーの堅牢化**: 
    - 「11_SPポリポリ」などのシートで、データ行内の数字（「1」「2」など）をヘッダーと誤認して解析が途切れる問題を修正。
    - 「カタログ№」や「色」ラベルの判定を厳格化し、ブロックの開始を正確に特定するように改善。
    - 横並びの価格テーブルをすべて検出できるよう、アンカー列（「売」列）の探索アルゴリズムを刷新。これにより、2kg等の特定の重量区分が欠落していた問題を解消。
    - 解析中の各ブロックの行インデックスを診断情報に含めるようにし、デバッグ性を向上。

### Fixed
- **SPマッチング精度の向上**: 解析ロジックの改善により、商品コードベースのマッチングにおいて、以前は欠落していた重量区分のデータも正しく参照されるようになりました。

## [Unreleased] - 2026-05-05

### Added
- **SP商品コードのデコード機能**: 9桁の商品コード（例: `008100501`）からカタログ番号（810）、重量（5kg）、形状（01=単袋）を抽出する機能を実装。これにより、より精度の高い自動マッチングが可能になりました。

### Changed
- **SPマスターパーサーの抜本的改良**: 
    - 複数テーブルが横並びになっている複雑なレイアウトのExcelシートから、重量やカタログ番号をテーブルごとに正確に抽出できるようロジックを刷新。
    - シート全体からのカタログ番号収集と、テーブル固有のコンテキスト抽出を組み合わせ、マッチングの網羅性を向上させました。
- **SPマッチングロジックの強化**: 
    - デコードされた商品コード情報を最優先で照合し、一致しない場合に従来の部分一致や重量ベースのフォールバックを行う二段階方式を導入。
    - マスターに一致しないSP商品に対しても、カスタム値上げ率が適用されるようフォールバック処理を追加。
    - 受注データの「SPオフセット」種別もSPマスター照合の対象として正確に処理されるよう調整。

## [Unreleased] - 2026-05-04

### Fixed
- **SP用マスター見積書の実装強化と不具合修正**:
    - `calculator.ts`: `SPMasterRow`のインポート不足を解消し、重複していたSP分岐を高度な「セレクトパック照合ロジック」に統一。
    - `calculator.ts`: シールブランチの閉じ括弧欠落によるロジックの崩れを修正。
    - `excelUtils.ts`: `SPMasterRow`/`SPMasterPrice`のインポートを追加し、`OrderRecord`のマッピングに`printCode`が欠落していた問題を修正。
    - `excelUtils.ts`: `mapRowToPriceMatrix`内の到達不能なデッドコードを削除。
    - `usePriceSimulation.ts`: `handleMasterUpload`内に残存していた孤立した`catch`ブロックを削除。
    - **TypeScript型エラーの解消**: 既製品マスター読み込み時や計算ロジック内の型不整合（キャスト不足等）を修正し、ビルドが通る状態に復旧。

## [Unreleased] - 2026-05-01

### Added
- **既製品マスターのExcel読み込み対応**: 既製品の単価改定データが含まれるExcelファイルを、直接マスターとして読み込めるように機能を拡張しました。
- **多様なシート名への対応**: 「既製品」「価格表」「マスター」「Readymade」などのシート名を自動的に検出し、パースを行うロジックを実装しました。
- **商品コードベースのパース**: Excelファイル内の商品コード（商品CD）をキーにして、通常単価やキャンペーン単価、数量スライド情報を抽出する機能を実装しました。
- **見積書からのマスター変換機能**: 本ツールで生成した「見積書」Excelを既製タブで読み込んだ際、自動的に商品コードベースの単価データに変換してマスターとして登録する機能を実装しました。

## [Unreleased] - 2026-04-27

### Refactored
- **UIコンポーネントの分割**: 巨大だった `page.tsx` を機能ごとに5つの独立したコンポーネント（SummaryDashboard, GroupPriceEditor, SimulationControls, OrderDataTable, HistoryAndMasterManager）に分割しました。
- **カスタムフックへのロジック抽出**: ビジネスロジックを `usePriceSimulation`、フィルタリング・検索ロジックを `useFilters` カスタムフックへ移行。UIとロジックを分離し、コードの再利用性とテスト性を向上させました。
- **データ型管理の強化**: 各コンポーネントのPropsに厳密な型を定義し、プロジェクト全体の型安全性を強化しました。

## [Unreleased] - 2026-04-14

### Fixed
- **Excel出力項目の改善**: 項目名「受注数」を「前回受注数」に変更し、見積上の文脈をより正確に表現するようにしました。
- **Excel出力のレイアウト修正**: 「改定率」および「営G改定率」がExcel上で左詰めになっていた問題を修正し、他の数値項目と同様に右詰め（右揃え）になるよう調整しました。
- **端数丸めロジックの改善**: 0.50単位などの端数丸めが「改定単価」のみに適用されるよう修正。内部的な「改定営G」の計算には、丸め前の理想的な利益増分を使用することで、利益額が意図せず切り捨て・切り上げられる現象を解消しました。
- **手入力値の尊重**: 表上でユーザーが直接入力した単価については、設定画面の丸めモードに関わらず入力値を優先し、そのまま維持するように変更しました。

### Added
- **実施時期の基準選択機能**: 改定の実施時期について「受注分より」「出荷分より」を切り替える機能を追加。選択結果はExcel見積書のフッターに反映されます。
- **利用者向けマニュアルの整備**: Web版の利用に特化した詳細なマニュアル（MANUAL.md）を新規作成。準備不要でブラウザから利用できることや、データの安全性を強調。

### Changed
- **READMEの刷新**: 一般ユーザーが直感的にWeb版やマニュアルへアクセスできるよう、構成を「利用方法重視」にリデザイン。開発者向けの高度なセットアップ手順は後半へ整理。

## [Unreleased] - 2026-04-13 (Update 3)

### Added
- **リスト内の値上げ率編集機能**: テーブル上の「値上率」を直接編集可能にしました。率を入力すると、合計コストに基づいた新単価が自動計算されます。単価の直接入力による率の再計算も維持され、双方向の調整が可能になりました。
- **UIコンポーネントの強化**: `InlineNumericInput` にサフィックス（%など）の表示機能を追加。非編集時のみ単位を表示し、クリーンな入力を維持します。

## [Unreleased] - 2026-04-13 (Update 2)

### Added
- **長期間注文なし商品のハイライト機能**: 「前回値上げ日」をツール上で設定することで、それ以降注文のない（最新受注日が古い）商品を自動的に赤字でハイライトする機能を実装。
- **エクセル読込の高度化**: エクセルの `最新受注日`（カラムT）の自動取得に対応し、日付形式の解析精度を大幅に向上。

## [Unreleased] - 2026-04-13 (Update 1)

### Added
- **読み取りファイル解除機能**: 読み込んだ受注データをクリアし、初期のファイル選択画面に戻るための「読み取り解除」ボタンを実装。誤操作防止のための確認ダイアログ付き。

### Changed
- **最新リポジトリとの同期**: リモートリポジトリ（GitHub）の最新の変更（シールタブ追加、カラムフィルタ機能、テーブルヘッダー固定など）をローカル環境に反映。

## [Unreleased] - 2026-04-10 (Update)

### Added
- **専用数値入力コンポーネント**: 入力中の値をローカルで制御し、確定時のみ状態を更新する `InlineNumericInput` を新規作成。

### Changed
- **数値入力の操作性改善**: 数値入力セルのクリック時に全選択する機能を追加。また、入力途中に値が勝手に書き換わったりデフォルトに戻ったりする現象を解消。
- **入力仕様の最適化**: ユーザーの要望に基づき、空欄確定時は `0` を代入、入力中はカンマなしで数値編集ができるように調整。

## [Unreleased] - 2026-04-09 (Update)

### Added
- **ライト/ダークモード切り替え機能**: ビジネス・ブルー基調のクリーンなライトモードをデフォルトとして実装し、ダークモードとの切り替えトグルを追加。
- **テーマ設定の永続化**: ユーザーが選択したテーマをブラウザ（localStorage）に保存する機能を実装。
- **顧客名の略称展開**: Excel出力時、顧客名に含まれる略称（(株)や(㈱)など）を自動的に「株式会社」へ展開する機能を実装。
- **Excel出力の改善（詳細情報の保持）**: 見積書のExcel出力に「営G」および「改定後 営G」列を追加。通常は非表示（折りたたまれた状態）にし、必要に応じて展開して確認できる構成に改善。
- **実施時期の選択機能**: UI上にカレンダー（日付選択）を追加し、Excelの見積書に具体的な実施予定日を反映できる機能を実装。未選択時は「別途ご相談」と表示。
- **直送先情報の表示**: 画面上の一覧およびExcel出力に「直送先コード」と「直送先名称」の列を追加し、配送情報の確認を容易にしました。また、Excel出力時は初期状態でこれらの列を折りたたむ（アウトライン）設定を追加しました。
- **営G改定率の追加**: Excel出力に「営G改定率」列を新設。計算式を埋め込み、営Gの金額変更に連動して率が自動計算されるようにしました。また、他の営G列と同様に折りたたみ設定を適用しました。

### Changed
- **ビジュアルデザインの刷新**: 信頼感のあるビジネスカラー（ブルー/スレート）を基調とした、より専門的なUIデザインへ移行。
- **「目に優しい」配色への調整**: 眩しさを抑えるため、ライトモードの背景を柔らかなオフホワイト（Stone 50系）に変更し、全体のコントラストを長時間作業向けに最適化。
- **視認性の向上**: ライトモード時に文字が背景と同化していた箇所（サマリー数値、入力項目等）の配色を修正し、全てのモードでクリアな表示を実現。
- **数値入力の挙動改善**: number型の入力フィールドにおいて、矢印キー（上下）による数値の増減を無効化。表内のナビゲーション機能を維持しつつ、意図しない数値変更を防止。
- **UIクリーンアップ**: 不要になったフローティングアクションボタン（出力ボタン）を削除し、画面レイアウトを整理。
- **ツール名の正式変更**: 利便性と分かりやすさを考慮し、ツール名を「値上げ見積書・作成ツール」から「価格改定見積書・作成ツール」に変更。

### Added
- **データの並び替え機能**: リスト全体を「材質名称 ＞ 重量 ＞ 色数」の優先順位で昇順に並べ替えるロジックを実装。

### Changed
- **別注・ポリ別注・既製品のエクセル出力最適化**: 「別注」「ポリ別注」に加え「既製品」の場合も、エクセル出力から「印刷コード」と「印刷代」を除外するように変更。
- **エクセル出力の挨拶文調整**: 挨拶文から「拝啓」および「敬具」を削除。長い文章が途切れないようセルの自動折り返し設定を追加するとともに、営G列の折りたたみ時を考慮して行の高さを十分なサイズ（30〜45px）に固定設定。
- **エクセル出力のページ設定改善**: A4横・1枚に収まる（Fit to Page）設定を追加し、印刷時の利便性を向上。
- **エクセル書式設定の堅牢化**: 列の動的な変更に対応するため、列キーに基づいた数値フォーマット適用ロジックに改善。

## [Unreleased] - 2026-04-08 (Update)

### Added
- **形状・受注数・印刷コード列の追加**: UIテーブルおよび見積書PDFに「形状」「受注数」「印刷コード（SPのみ）」列を追加。
- **SP・シルクタブの表示改善**: 商品名の表示ソースをExcelの「タイトル」列（U列）に切り替え可能に。
- **商品名自動短縮機能**: 長い詳細名称（SPタイトル）から記号や記述スペックを自動除去し、核心的なブランド名のみを抽出して表示するロジックを実装。

### Changed
- **UIレイアウトの最適化**: 「印刷代」列を「印刷営G」の直前に移動し、印刷関連の情報をグループ化。
- **PDFレイアウトの調整**: 列の増加に伴い、見積書PDFの列幅を再計算して視念性を確保。

### Fixed
- **ESLintの警告解消**: コード品質向上のため、全ての `any` 型を具体的な型定義へ差し替え、未使用インポートを削除。
- **CIパスの確保**: GitHub ActionsのCIを確実に通過させるためのコードクリーンアップ。

